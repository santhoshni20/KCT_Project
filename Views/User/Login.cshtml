@* @model KSI_Project.Models.DTOs.UserLoginDTO
@{
    ViewData["Title"] = "Login";
    ViewData["NoSidebar"] = true;
}

<div class="container mt-5">
    <h2>Login</h2>
    <form asp-action="Login" method="post">
        <div class="mb-3">
            <label>Roll Number / College Mail</label>
            <input asp-for="rollOrMail" class="form-control" required />
        </div>
        <div class="mb-3">
            <label>Password</label>
            <input asp-for="password" type="password" class="form-control" required />
        </div>
        <button type="submit" class="btn btn-success">Login</button>
    </form>

    @if (ViewBag.ErrorMessage != null)
    {
        <div class="alert alert-danger mt-3">@ViewBag.ErrorMessage</div>
    }

    <p class="mt-3">Don't have an account? <a asp-action="Signup">Signup here</a></p>
</div> *@
